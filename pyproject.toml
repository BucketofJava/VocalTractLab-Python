[build-system]
requires = ["setuptools", "wheel", "numpy>=1.19.0", "Cython>=0.29.24", "cmake>=3.21.1"]
build-backend = "setuptools.build_meta"
[tool.setuptools_scm]
version_scheme = "guess-next-dev"
local_scheme = "dirty-tag"